#!/bin/zsh

set -e

source "$(dirname "$0")/../lib/paths"

echo "Setting up Eza..."

rm -rf ~/Library/"Application Support"/eza
ln -snf "$DOTFILES_DIR/Library/Application Support/eza" ~/Library/"Application Support"/eza
