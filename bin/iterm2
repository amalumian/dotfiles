#!/bin/zsh

set -e

source "$(dirname "$0")/../lib/paths"

echo "Setting up iTerm2..."

rm -f ~/.config/iterm2/com.googlecode.iterm2.plist
ln -snf "$DOTFILES_DIR/.config/iterm2/com.googlecode.iterm2.plist" ~/.config/iterm2/

