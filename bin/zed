#!/bin/zsh

set -e

source "$(dirname "$0")/../lib/paths"

echo "Setting up Zed..."

rm -f ~/.config/zed/settings.json
ln -snf "$DOTFILES_DIR/.config/zed/settings.json" ~/.config/zed/
rm -f ~/.config/zed/keymap.json
ln -snf "$DOTFILES_DIR/.config/zed/keymap.json" ~/.config/zed/
